# Changelog

## [0.1.0] - 2025-11-18
- feat: finalize the public API by ensuring `calc_expression` exposes rounded, trailing-zero-trimmed `Decimal` results and publish-ready exports through `__all__` in `calculator`.
- feat: capture usage guidance, percent-aware syntax rules, release checklist, and automated release-note workflow in `README.md`.
- chore: add testing/lint tooling (`pytest`/`ruff`), CI workflow, MIT license, `.gitignore`, and complete package metadata for `py-easy-calc`.

## [Unreleased]
-
